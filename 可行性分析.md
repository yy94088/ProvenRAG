# 异构证据图检索模型

---

## 1. 模型解读：它在解决什么问题

### 1.1 一句话概括

这是一个**面向多跳问答/事实核查的图式 RAG 检索器**。它把「召回 → 构建证据图 → 挖掘信息密度最高且来源独立的子图 → 喂给 LLM」这条流水线，从"最大化相关性/连通性"升级为"最大化**每 token 的独立、互补证据量**"，并显式惩罚转载/重复带来的假密度。

### 1.2 它针对的核心失败模式

文件里命名为 **Correlated Evidence Amplification（相关证据放大）**：

> 在语义图里，大量近重复文档聚集的区域天然最"稠密"。一条错误新闻被转载 20 次后，普通的 densest-subgraph retriever 会因为这些重复节点形成的高连通簇，而**偏向错误答案**，压过唯一的一条一手正确来源。

这是一个真实且值得写的问题：图密度类方法（densest subgraph、PPR、GNN-RAG）都隐含"边多 = 证据多"的假设，但边多也可能是**同一个来源的 20 份拷贝**。

### 1.3 核心洞察（论文辨识度所在）

| 传统方法的目标 | 本模型的目标 |
|---|---|
| 最大化 $\|E(S)\|/\|V(S)\|$（边密度） | 最大化 **Independent Evidence Density**（独立来源覆盖 + 推理槽位覆盖 + 跨来源支持） |
| 重复节点会人为抬高密度 | 通过 provenance 收缩 / max 聚合实现 **duplication-invariance** |
| 只要"连通" | 要求"来源独立 + 推理角色互补 + 支持边一致" |

这三条（新失败模式、新目标、可形式化的不变性性质）构成了一个完整、自洽、可发表的贡献闭环。

---

## 2. 总体可行性判断

**结论：设计可行，且方向聚焦、贡献清晰，具备发表潜质。** 具体评估分三层：

| 维度 | 判断 | 说明 |
|---|---|---|
| 问题真实性 | ✅ 高 | 重复/转载污染检索是 RAG 的真实痛点，MAGIC 等基准已佐证多跳冲突检测之难 |
| 技术可行性 | ✅ 高 | 每一步都有成熟组件（BM25/dense、NLI、NER、MinHash、densest subgraph），无不可逾越的技术障碍 |
| 理论可行性 | ✅ 中-高 | duplication-invariance 可形式化为定理，前提是**严格定义收缩映射与不变性**（§5） |
| 工程可行性 | ⚠️ 中 | 边构建（尤其 NLI/LLM 边）有成本，需 cheap-filter → 昂贵模型的级联（§3.3） |
| 新颖性 | ✅ 中-高 | 与 SimGRAG/SubgraphRAG/GNN-RAG 的差异需写透，落脚点放在 provenance + independent density（§7） |

**主要风险**：目标函数精确求解 NP-hard（需近似 + 证明）；"独立证据"无直接 gold 标签（用 supporting-fact + provenance group 作代理）；边构建规模与成本。

**建议范围**（文件已自评）：第一版**聚焦「重复证据放大 + provenance-aware density」**，把 signed coalition 与 motif matching 作为第二/三阶段扩展。这个范围控制是正确的——单一清晰的问题比什么都做更有竞争力。

---

## 3. 逐步方法详解（核心）

以下按流水线的 8 个阶段，逐一给出**推荐方法 + 备选 + 落地细节**。

### Step 0（可选扩展）问题 → 轻量 reasoning motif

**目标**：把问题解析成一个结构化的推理骨架，用于后续的推理槽位覆盖与 motif 匹配。

**推荐方法**：

- 用 LLM few-shot 将问题映射为 5 类 motif 之一：
  - `chain`：A → B → answer
  - `comparison`：两个实体分别连接到可比较属性
  - `intersection`：两个条件共同指向答案
  - `temporal`：事件与时间约束
  - `support/refute`：claim 与证据立场
- 同时抽取 motif 中的**角色实体/槽位**（如 `{source: A, hop: B, target: answer}`），这些槽位直接作为 §3.6 里 `ReasoningCoverage` 的覆盖集合。

**落地细节**：few-shot 模板参照 SimGRAG 的 12-shot 三元组模式生成（见 §7），但输出是"角色槽位"而非"实体-关系三元组"。第一版可**不做 motif 匹配，只把槽位用于 Coverage 项**，降低复杂度。

---

### Step 1：Hybrid Retrieval（候选召回 100–300 条）

**目标**：用便宜的稀疏+稠密混合召回，得到候选证据池。这一步追求**高召回**（宁多勿漏），精确性交给后面的子图挖掘。

**推荐方法**：

- **稀疏**：BM25（Pyserini / Elasticsearch）。
- **稠密**：Contriever、BGE、E5 或 GTE 系列双塔编码器；对多跳问题，可对**问题分解后的子问题**分别召回再合并。
- **可选 late-interaction**：ColBERT（成本更高，可作对比项而非主检索器）。

**融合方式**：
- Reciprocal Rank Fusion（RRF，$k=60$）：$\text{RRF}(v)=\sum_{r\in\text{rankers}}\frac{1}{k+\text{rank}_r(v)}$，无需调分数量纲。
- 或归一化分数加权求和。

**规模控制**：取 top 100–300 作为后续图构建的节点候选。这是后续 $O(n^2)$ 边构建的规模上限，300 个节点已经是性价比拐点。

---

### Step 2：节点构建（document / chunk / atomic claim）

**目标**：确定图节点的粒度。

**推荐方法（两层结构，实测再取舍）**：
- **chunk 节点（粗）**：128–256 token 分块，重叠 20–30%。用于边构建与子图挖掘，效率高。
- **claim 节点（细）**：用 LLM few-shot 把每个 chunk 分解为**原子 claim**（主语-谓词-宾语三元组，或一句话一个事实），用于精确的 support/contradict 判断。
- **两者之间加 `contained-in` 边**：claim 属于某个 chunk。

**claim 抽取备选**：OpenIE、LLM 抽取、或 NLI 风格的事实性分解。**推荐 LLM few-shot**（质量最好），但只对进入候选池的 chunk 抽取，控制成本。

> 工程建议：第一版可以先只用 chunk 节点跑通全流水线，claim 节点作为消融/精度增强。claim 节点最大的价值在 contradict 边和冲突定位。

---

### Step 3：边构建（4 类异质边，逐类给方法）

这是全模型**成本最高、也最决定质量**的一环。统一策略：**cheap filter 先筛候选对，昂贵模型只处理幸存对**。

#### 3.1 `support` 边（两证据互相支持 / 共同完成一个推理步）

- **cheap filter**：两节点共享实体（NER）、BM25/embedding 相似度超过阈值。
- **精确判断**：
  - **NLI/entailment**：DeBERTa-large-MNLI、Flan-T5 entailment，判断 claim 对 claim 的蕴含方向。
  - **LLM-as-judge**：对高价值候选对做"是否相互支持"二分类（更强、更贵，用于精标注或小规模）。
- **方向/权重**：可作无向带权（支持度 = entailment 概率），或保留有向（A 蕴含 B）。

#### 3.2 `bridge` 边（共享实体/时间/因果，负责多跳连接）

三类 bridge 边，各自方法：

| bridge 类型 | 方法 |
|---|---|
| 共享实体 | NER（spaCy/Flair，或 LLM NER）+ 实体链接；两节点共享实体即建边，边属性 = 共享实体集 |
| 共享时间 | 时间表达式抽取+归一化（SUTime/HeidelTime 或 LLM），共享/可比时间即建边 |
| 因果关系 | 有监督因果抽取器，或 LLM few-shot cause-effect 抽取；启发式（"导致/引起/thus/因此"连接词）作 cheap filter |
| 共指（补充） | 共指消解（longformer-coref 或 LLM），把代词/指代链接到实体，增强多跳 |

#### 3.3 `contradict` 边（两 claim 矛盾）

- **NLI contradiction**：claim 对 claim 的 NLI，`contradiction` 标签 → 负边。
- **数值/属性冲突检测**：同一主体、不同属性值（"生于 1980" vs "生于 1990"）。
- **LLM 立场判断**：support / refute / neutral 三分类。
- **高相似 + 相反结论**：embedding 相似度高但含否定词/反义词（"有效" vs "无效"）→ 矛盾候选。

#### 3.4 `dependency` 边（转载/引用/同源/近重复改写）

这是本模型**最关键、也最好做**的一类边，直接支撑 duplication-invariance。

| dependency 类型 | 方法 |
|---|---|
| 逐字近重复 | MinHash + LSH（Jaccard > 0.8）、SimHash（海明距离 < k） |
| 改写/释义 | ParaphraseBERT / LLM 判断；embedding cosine > 0.95 作 cheap filter |
| 转载/引用 | URL 规范化（去 query 参数、canonical 域名）+ 标题指纹 + "转载自/来源"模式匹配 |
| 同源 | 文档元数据（site、author、publisher、same original source） |

**边构建工程总则**：300 节点 → 至多 ~45k 候选对。先用 **embedding 相似度 + 共享实体 + MinHash** 三类 cheap filter 把候选对降到几千对，再对幸存对上 NLI/LLM。这样成本可控。

---

### Step 4：识别来源依赖/转载关系，收缩 provenance group

**目标**：把"同一来源的 N 份拷贝"折叠成一个 provenance group，消除重复计数。

**两种方案（文件已指出，建议都实现并对比）**：

1. **硬收缩（super-node）**：以 dependency 边（及高相似边）建图，取连通分量；每个分量收缩为一个 super-node，其 relevance = 成员 max 或 mean。收缩后图结构变小，后续所有计算在收缩图上进行。
2. **软聚合（max / 饱和函数）**：不真正合并节点，但在目标函数里对同一 group 的支持分数做 $\max(\cdot)$ 或饱和函数聚合，使"十份转载不能贡献十次支持分"。

**推荐组合**：主流程用**硬收缩**（更干净、定理更好写），保留 group 内成员信息用于 `provenance-group coverage` 指标（§8.3）。软聚合作为消融对比。

**过合并风险**：阈值过高会把"确实不同来源但内容相似"的节点误并。应对：阈值设置严格（如 Jaccard > 0.85 才归同组），并在实验里做阈值敏感性分析。

---

### Step 5：构造 query-conditioned signed evidence graph

**目标**：在图里注入问题相关性，并标记边的符号。

- **节点特征**：`r_q(v)` = 节点对问题的相关性，取 dense 检索分数、BM25 分数，或用 cross-encoder reranker（如 monoT5、BGE-reranker）重排后分数。可选：对 claim 节点，r_q 用 claim 与问题的 entailment 分数。
- **边符号**：
  - `support` / `bridge` → 正边（权重 = 置信度）
  - `contradict` → 负边（权重 = 置信度）
  - `dependency` → 不进"支持"信号，而是进惩罚项（见 §3.6 的 Dep 项）
- **query-conditioned 的含义**：边权重可乘上 query 相关因子（如两端节点相关性较低时，边的信息增益也低），使图真正"以问题为条件"。

---

### Step 6：挖掘预算约束的独立证据稠密子图（**核心优化**）

这是论文的技术心脏。目标函数（原式）：

$$
\max_{S:\ \operatorname{tokens}(S)\le B}
\frac{\sum_{v\in S}r_q(v) + \alpha\,\mathrm{Support}(S) + \beta\,\mathrm{ReasoningCoverage}(S)}{\operatorname{tokens}(S)}
- \gamma\,\mathrm{Dependency}(S) - \delta\,\mathrm{Redundancy}(S)
$$

#### 6.1 各项的精确定义（需要你在论文里写清楚）

| 项 | 定义建议 |
|---|---|
| $r_q(v)$ | 节点相关性（§5） |
| $\mathrm{Support}(S)$ | **跨 provenance group** 的支持边权重和（组内贡献用 max 聚合，见 §4）；保证 duplication-invariance |
| $\mathrm{ReasoningCoverage}(S)$ | 覆盖了多少**推理槽位**（Step 0 的 motif 角色/实体/子问题）。可用加权覆盖：$\sum_j w_j \cdot \mathbb{1}[\text{槽位}j\text{被}S\text{覆盖}]$，也可用次模覆盖（边际递减） |
| $\mathrm{Dependency}(S)$ | 组内依赖/重复边的惩罚（同一 group 内成员之间的依赖边权重和） |
| $\mathrm{Redundancy}(S)$ | 跨组信息冗余（如 ROUGE-L 或 embedding 相似度的跨组重叠） |
| $\operatorname{tokens}(S)$ | 把 $S$ 序列化喂 LLM 的 token 数（节点文本长度 + 引用/角色标注开销） |

#### 6.2 复杂度与求解策略

该问题是 **ratio 目标 + knapsack 预算 + 图结构项** 的组合，**精确求解一般 NP-hard**。给出三种可写进论文的求解路径：

**方案 A — 参数化搜索（Dinkelbach / binary search on λ）**（推荐作为理论/近似保证的主方法）

把 ratio 目标转化为参数化判定：对给定 $\lambda$，定义节点得分

$$
g_\lambda(v) = r_q(v) + \alpha\,s_v + \beta\,c_v - \lambda\,c_{tok}(v) - \gamma\,d_v - \delta\,red_v
$$

其中 $s_v, c_v, d_v, red_v$ 是各项在节点上的局部化。二分 $\lambda$，每次在预算约束下最大化 $\sum_{v\in S} g_\lambda(v)$（一个 knapsack / 贪心问题），直到收敛。可给出近似比。

**方案 B — 贪心 peeling（Charikar 风格，工程首选）**

```
输入: 收缩图 G, 节点 token 成本 c(v), 预算 B, 相关/支持/覆盖/依赖/冗余项
输出: 最优节点集 S*

1. S ← 全部候选节点（诱导子图）
2. f(S) ← 按目标函数计算初始值（组内贡献用 max 聚合）
3. S* ← S; best ← f(S)
4. while S 非空:
5.     对每个 v ∈ S 计算移除后的边际目标 Δ(v) = f(S) − f(S \ {v})
6.     v* ← argmin_v  Δ(v) / c(v)        # 单位 token 贡献最小者先剔除
7.     S ← S \ {v*}
8.     若 tokens(S) ≤ B 且 f(S) > best:  S* ← S; best ← f(S)
9. return S*
```

这是标准 densest-subgraph 贪心 peeling 在"带 token 成本 + 自定义分子分母"下的直接推广，实现简单、可控，适合第一版跑通。

**方案 C — 子模最大化视角**

若 $\mathrm{Support}$、$\mathrm{ReasoningCoverage}$ 是单调子模函数、$\mathrm{Dependency}$/$\mathrm{Redundancy}$ 是超模惩罚，则目标可改写为 knapsack 约束下的子模最大化，用 **cost-benefit 贪心**，可证 $(1-1/e)$ 型近似。适合作为论文的理论深度点，但需先证明各项的子模性（Support 通常天然子模，Coverage 是标准子模，Dep/Red 需构造为超模）。

**落地建议**：第一版用 **方案 B（贪心 peeling）** 跑通 + 与 **方案 A（参数化）** 对比，方案 C 作为可选的近似保证扩展。

#### 6.3 基线（必须实现，用于对比"传统图密度"）

- 普通 densest subgraph：Goldberg max-flow 精确解，或 Charikar 2-近似贪心。
- Steiner tree：连接问题实体的最小树（多跳桥接）。
- Personalized PageRank（PPR）：从问题实体出发的 top-k 节点。
- 这些基线正好是"重复证据放大"失败模式的对照——它们都会被重复节点误导，从而凸显你的方法。

---

### Step 7：按推理拓扑序列化给 LLM

**目标**：把子图 $S$ 变成 LLM 可读、可引用的上下文，并控制在 token 预算 $B$ 内。

**推荐方法**：
- **拓扑排序**：对 $S$ 的诱导子图按桥接方向拓扑排序（chain 结构里源→中间→目标）；无桥接时按 relevance + 角色排序。
- **角色标注**：每个节点前加角色/立场/来源标注，如 `[E3 | provenance=g1 | stance=支持 | hop=1]`。
- **引用 ID**：为每个节点分配 `[E_i]` 标签，供 LLM 输出引用。
- **token 控制**：超过 $B$ 时按单位 token 贡献降级/截断（与 §6.2 的贪心排序一致）。

**格式示例**：

```
[E1 | 来源组 g1 | 支持] 慕尼黑是巴伐利亚州首府。
[E2 | 来源组 g2 | 桥接] 巴伐利亚州位于德国南部。
[E3 | 来源组 g3 | 支持] 德国南部的首府是慕尼黑。
```

---

### Step 8：回答、引用、置信度或拒答

- **回答 + 引用**：LLM 生成答案，并要求引用 `[E_i]`。用数据集的 supporting-fact 标注判定引用正确性（§8.3）。
- **置信度**：
  - self-consistency（采样 $k$ 次，多数一致性比例）。
  - token-level 概率 / LLM 自报概率。
  - 可选 ECE 校准（temperature scaling）。
- **拒答/冲突**（第一版可只做开关，第二版做 signed coalition）：
  - 若挖掘出支持性冲突（$S^+$ 与 $S^-$），比较两者可靠性；差距 $<\varepsilon$ → 输出**冲突说明**或**拒答**。
  - 这是 selective prediction / abstention 的标准做法，可接 selective risk 指标。

---

## 4. 理论性质：duplication-invariance（贡献 3 的落地）

这是论文里"可以形式化为定理"的点，也是与 Top-K / PPR / 普通 dense subgraph 拉开差距的关键。形式化思路：

**定义（provenance 收缩）**：设 $\pi$ 为把同一 provenance group 的节点折叠为 super-node 的映射，收缩后的图为 $\pi(G)$。

**定理（duplication-invariance，非正式陈述）**：

> 若新增文档 $v'$ 是已有 provenance group $g$ 的**完全重复或改写**，则 $\pi(G \cup \{v'\}) \cong \pi(G)$；进一步，若目标函数中 $\mathrm{Support}(S)$ 对组内成员采用 $\max$/饱和聚合、$\mathrm{Dependency}(S)$ 正确惩罚组内冗余，则
> $$\arg\max_S f(S) \text{ 在 } G \cup \{v'\} \text{ 上与在 } G \text{ 上给出相同检索结果。}$$

**证明要点**：
1. $v'$ 收缩进 $g$，$\pi$ 不变 → 图结构不变。
2. 分子三项（relevance/support/coverage）在 $\max$ 聚合下对组内复制不变。
3. 分母 $\operatorname{tokens}(S)$ 只随"选入的独立证据"增长，复制不改变"选不选"的边际决策（复制节点与被复制节点贡献相同但受依赖惩罚）。

**对比（写进论文的表）**：

| 方法 | 具备 duplication-invariance？ | 为何不具备 |
|---|---|---|
| Top-K | ❌ | 重复会挤占 rank，排挤真证据 |
| PPR | ❌ | 重复节点分走概率质量 |
| 普通 densest subgraph | ❌ | 重复节点形成高连通团，人为抬高密度 |
| 本模型 | ✅ | provenance 收缩 + max 聚合 + 依赖惩罚 |

> 注意：写定理时要**严格**。需明确"完全重复/改写"的精确定义（用 MinHash Jaccard 阈值还是 embedding 阈值）、"相同检索结果"的精确定义（S 的收缩等价类不变）。这是审稿人最容易挑刺的地方。

---

## 5. 冲突感知扩展（第二阶段）

文件建议作为第二阶段，判断正确。方法预留：

- **signed dense coalition 挖掘**：在带符号图上找两个簇 $S^+$（簇内以支持边为主）与 $S^-$（簇内以支持边为主），**簇间以矛盾边为主**。
- **方法**：correlation clustering、sign-aware densest subgraph、或谱方法（负边作组间惩罚）。
- **判定拒答**：$\mathrm{reliability}(S^+) \approx \mathrm{reliability}(S^-)$ 时输出冲突说明/拒答。
- **依赖基准**：MAGIC（EMNLP Findings 2025，多跳图结构 inter-context 冲突检测）可作为主要评测集；文件另引用的 EMNLP 2025 多文档性能下降 ~20% 的结果可作为动机佐证。

---

## 6. Subgraph Matching 的差异化定位（避免被 SimGRAG 稀释）

文件已准确指出：**SimGRAG（Findings of ACL 2025）已经做了 query-to-pattern + pattern-to-subgraph**，所以 motif matching 本身不能当主创新。

**差异化要点（写进论文）**：

| 维度 | SimGRAG | 本模型 |
|---|---|---|
| 匹配对象 | KG 上的**实体-关系模式**（三元组 GSD 距离） | 证据图上的**推理角色 + 独立证据结构** |
| 图性质 | 结构化 KG，模式显式 | 异构证据图，来源/立场/依赖显式建模 |
| 核心目标 | 语义+结构匹配距离最小 | 独立证据密度最大（provenance-aware） |
| 鲁棒性 | 未处理重复/转载 | duplication-invariance 为核心性质 |

即：**匹配的是"推理角色与独立证据结构"，而不是"实体关系模式"**。motif 用于提供推理槽位（Coverage 项）与排序约束，而不是复制 SimGRAG 的匹配机制。

---

## 7. 实验设计落地

### 7.1 数据集三组

| 组 | 数据集 | 用途 |
|---|---|---|
| 多跳 QA | HotpotQA、2WikiMultiHopQA、MuSiQue | 主评测 + supporting-fact |
| KGQA | WebQSP、ComplexWebQuestions | 泛化到 KG 场景 |
| 冲突与鲁棒性 | MAGIC、RAMDocs + 自建 CopyBurst | 冲突检测 + 重复鲁棒性 |

### 7.2 基线（完整清单，分组实现）

- 稀疏/稠密：BM25、Contriever、ColBERT
- 图式 RAG：HippoRAG、KG²RAG、G-Retriever、GRAG、GNN-RAG、SubgraphRAG、SimGRAG
- 经典图算法：普通 densest subgraph、Steiner Tree、PPR

### 7.3 指标（文件要求，逐一给实现方案）

| 指标 | 实现方案 |
|---|---|
| EM / F1 | 标准 QA 指标 |
| supporting-fact Recall/F1 | 用数据集的 supporting-fact 标注，判断检索到的节点是否命中支持事实 |
| 每 1K token 有效证据量 | 命中 supporting fact 数 / 实际 token 消耗，直接呼应"每 token 独立证据"主题 |
| provenance-group coverage | 检索结果覆盖了多少个不同 provenance group（衡量来源多样性） |
| 重复次数增长时的性能下降曲线 | CopyBurst 里重复次数 1/5/10/20 时的 EM/F1 曲线 |
| conflict detection | 在 MAGIC 上测冲突检测/定位准确率 |
| ECE / selective risk | 置信度校准 + 拒答的风险-覆盖率曲线 |
| 检索延迟 + 子图规模 | 端到端时延、$\|S\|$、$\operatorname{tokens}(S)$ |

### 7.4 CopyBurst 压力测试（benchmark contribution）

**构造方法**：
1. 取 HotpotQA / 2WikiMultiHopQA / MuSiQue 的 **distractor**（干扰项）。
2. 对 distractor 做 **1 / 5 / 10 / 20 次**复制，以及**释义改写**（LLM paraphrase 或 back-translation），生成近重复但非逐字的变体。
3. **三种操纵**：分别复制（a）正确证据、（b）错误证据、（c）无关证据。
4. 测：答案正确率随重复次数的变化曲线。

**论文价值**：这直接验证"duplication-invariance"这一核心假设，比单纯再报一次 QA accuracy 更有说服力，且是可直接复用、可被后续工作引用的 benchmark 贡献。

---

## 8. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 边构建成本高（NLI/LLM 边） | 高 | cheap filter 级联；只对候选对上昂贵模型；claim 节点仅对候选池抽取 |
| 目标精确求解 NP-hard | 中 | 用贪心 peeling + 参数化搜索，给出近似比；不追求精确解 |
| duplication-invariance 定理难写严 | 中 | 严格定义"重复/改写"与"结果不变"；把阈值的敏感性作为实验项 |
| 新颖性被 SimGRAG/SubgraphRAG 稀释 | 中 | 差异写透（§6），落脚 provenance + independent density |
| "独立证据"无 gold 标签 | 中 | 用 supporting-fact + provenance group 作代理指标；CopyBurst 直接验证不变性 |
| 工程量大、周期长 | 中 | 分阶段（见 §9），MVP 先做 QA + CopyBurst |

---

## 9. 工作量与里程碑（建议）

| 阶段 | 内容 | 产出 |
|---|---|---|
| M1（MVP） | hybrid retrieval + 节点 + dependency/近重复边 + provenance 收缩 + 贪心 peeling 子图 + LLM 回答 | 端到端跑通，QA 基线可跑 |
| M2 | support/bridge/contradict 边 + 完整目标函数 + 贪心 vs 参数化对比 | 主结果 + 消融 |
| M3 | duplication-invariance 定理 + CopyBurst 构造 + 重复鲁棒性实验 | 核心贡献闭环 |
| M4 | 全部基线 + 全部指标 + KGQA/冲突数据集 | 论文实验完备 |
| M5（可选扩展） | signed coalition + motif matching | 第二/三阶段论文 |

---

## 10. 结论

1. **模型设计可行**：问题真实、贡献闭环完整（新失败模式 → 新目标 → 可证性质 → 专用 benchmark），每一步都有成熟方法支撑，无技术死点。
2. **最重要的三个落地决策**：
   - 第一版**收缩范围**到「重复证据放大 + provenance-aware density」，signed coalition / motif 留作扩展（文件自评正确）。
   - 目标函数用 **贪心 peeling + 参数化搜索**求解，并**严格形式化 duplication-invariance 定理**。
   - 边构建用 **cheap filter → 昂贵模型** 级联控制成本。
3. **最有辨识度的贡献**是 `Independent Evidence Density` 目标 + `duplication-invariance` 性质 + `CopyBurst` benchmark，三者互相印证，构成一个可发表且可被引用的完整工作。
