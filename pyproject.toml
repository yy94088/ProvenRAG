[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "provenrag"
version = "0.1.0"
description = "Minimal provenance-aware evidence selection prototype"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["provenrag"]

